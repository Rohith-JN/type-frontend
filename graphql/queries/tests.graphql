query Tests($uid: String!,$limit: Int!, $cursor: String) {
  tests(uid: $uid, limit: $limit, cursor: $cursor) {
    tests {
      id
      creatorId
      time
      accuracy
      wpm
      chars
      createdAt
    }
    hasMore
  }
}