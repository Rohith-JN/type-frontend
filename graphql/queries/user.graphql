query User($uid: String! ) {
    user( uid: $uid ) {
        error {
            field
            message
        }
        user {
            username
            email
            createdAt
        }
    }
}